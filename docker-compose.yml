services:
  talktalk:
    build:
      context: .
      dockerfile: Dockerfile
    image: talktalk:latest
    container_name: talktalk
    restart: unless-stopped
    environment:
      TALKTALK_HOST: 0.0.0.0:8080
    ports:
      - "8080:8080"
